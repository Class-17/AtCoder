#include <bits/stdc++.h>

using i64 = long long;

constexpr int K = 27;

int main() {
    std::cin.tie(nullptr)->sync_with_stdio(false);
    int n; std::cin >> n;
    std::vector<i64> a(n);
    for (auto &x : a) std::cin >> x;
    i64 res = 0;
    std::map<int, int> cnt;
    i64 sum[K]{};
    for (auto &x : a) {
        for (int i = 0; i < K; ++i)
            
    }
    return 0;
}